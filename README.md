# Password Generator
Command-line password generator written in Python.
There are two mode available - interactive and CLI mode.

## Features
- Generation password with given length
- User-controlled password generation with digits, special characters, upper cases
- Writing generated password to a file 

## Usage
Interactive mode:
python generator.py

CLI mode
python generator.py --length [LENGTH] --no_digits --no_special --no_upper